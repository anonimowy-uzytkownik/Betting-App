<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/cardview_dark_background">

    <EditText
        android:id="@+id/editTextNewPassword"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:layout_above="@+id/editTextNewPasswordRepeated"
        android:layout_centerInParent="true"
        android:layout_marginTop="15dp"
        android:gravity="center"
        android:hint="new password"
        android:inputType="textPassword"
        android:textColor="#050505"
        android:background="@drawable/edit_text_border"
        />

    <EditText
        android:id="@+id/editTextNewPasswordRepeated"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:layout_centerInParent="true"
        android:layout_marginTop="15dp"
        android:gravity="center"
        android:hint="repeat new password"
        android:inputType="textPassword"
        android:textColor="#050505"
        android:background="@drawable/edit_text_border"
        />



    <Button
        android:id="@+id/buttonPasswordChange"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:layout_below="@+id/editTextNewPasswordRepeated"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:text="SAVE CHANGES"
        android:textColor="#A6F1CC68"
        android:textSize="20sp"
        android:textStyle="bold"

        />

</RelativeLayout>